export type Point2D = { x: number; y: number }

export type Theme = {
  bg: string
  text: string
  border: string
  axis: string
  tick: string
  tickText: string
  sidebar: string
  sidebarActive: string
}
